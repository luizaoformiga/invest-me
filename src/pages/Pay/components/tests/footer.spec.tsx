import { render } from "@testing-library/react";
import { FooterPay } from "../footer";

describe("FooterPay", () => {
  test("Deve renderizar o component FooterPay", () => {
    render(<FooterPay />);
  });
});
