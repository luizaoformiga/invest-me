import { render } from "@testing-library/react";
import { HeaderPay } from "../header";

describe("HeaderPay", () => {
  test("Deve renderizar o component HeaderPay", () => {
    render(<HeaderPay />);
  });
});
