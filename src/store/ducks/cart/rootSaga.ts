export const i = []/**import { all, AllEffect, ForkEffect } from "redux-saga/effects";
import * as saga from "./sagas";

export default function* rootSaga(): Generator<
  AllEffect<AllEffect<ForkEffect<never>>>,
  any,
  unknown
> {
  return yield all([saga]);
}
 */